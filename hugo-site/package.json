{
  "private": true,
  "scripts": {
    "build": "hugo --gc --minify && pagefind --site public",
    "preview": "hugo server --disableFastRender"
  },
  "devDependencies": {
    "pagefind": "^1.5.0"
  }
}
